{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"],
    /* Run through vite-node, never emitted — so checked, not built. */
    "noEmit": true,
    "composite": false
  },
  "include": ["**/*.mts"]
}
