# AGENTS.md

See [CLAUDE.md](./CLAUDE.md). It carries the commands, the architecture, and
the handful of ideas worth understanding before changing anything — how
answers are kept from leaking, why storage is split across blobs, and how the
phase machine advances without a scheduler.

Kept as a pointer rather than a copy so the two cannot drift.
